SCS Tool Add-On
======

Assigns basic SimpleClaimSystem functionality to an in-game tool. By default, a Golden Axe. Can be configured in the plugin config.

Build
-----

Run:

```bash
mvn package
```

Copy the generated JAR from `target/` into your Paper server's `plugins/` folder.
